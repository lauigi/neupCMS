neupCMS_config={
    'user':'root',
    'db':'db_name',
    'passwd':'db_password',
    'host':'localhost',
    'charset':'utf8',
    'secret_key':'text your own string here'
    'MEDIA_ROOT':'''your own media root.a sample is "os.path.join(os.getcwd(),'media/').replace('\\','/')",don't forget import os module if need'''
}
